package com.cyou.mrd.pengyou.viewcache;

import android.view.View;
import android.widget.Button;
import android.widget.ImageButton;
import android.widget.LinearLayout;
import android.widget.ProgressBar;
import android.widget.RatingBar;
import android.widget.TextView;

import com.cyou.mrd.pengyou.R;
import com.cyou.mrd.pengyou.widget.RoundImageView;

public class IntroCommentItemViewCache {

	private View mView;
	private RoundImageView mAvatarIV;
	private TextView mNickNameTV;
	private RatingBar mRB;
	private TextView mContentTV;
	private TextView mDateTV;
	private TextView mCommentCountTV;
	private LinearLayout mCommentLL;
	private TextView mSubCommentTV;
	private Button mPackUpBtn;
//	private ImageButton mCommentBtn;
	private ProgressBar mPB;
	private TextView mEmptyTV;
	private LinearLayout mSubCommentLL;
	
	public IntroCommentItemViewCache(View view){
		this.mView = view;
	}

	public RoundImageView getmAvatarIV() {
		if(mAvatarIV == null){
			mAvatarIV = (RoundImageView)mView.findViewById(R.id.intro_comment_item_avatar_iv);
		}
		return mAvatarIV;
	}

	public TextView getmNickNameTV() {
		if(mNickNameTV == null){
			mNickNameTV = (TextView)mView.findViewById(R.id.intro_comment_item_nickname_tv);
		}
		return mNickNameTV;
	}

	public RatingBar getmRB() {
		if(mRB == null){
			mRB = (RatingBar)mView.findViewById(R.id.intro_comment_item_rb);
		}
		return mRB;
	}

	public TextView getmContentTV() {
		if(mContentTV == null){
			mContentTV = (TextView)mView.findViewById(R.id.intro_comment_item_content_tv);
		}
		return mContentTV;
	}

	public TextView getmDateTV() {
		if(mDateTV == null){
			mDateTV = (TextView)mView.findViewById(R.id.intro_comment_item_date_tv);
		}
		return mDateTV;
	}

	public TextView getmCommentCountTV() {
		if(mCommentCountTV == null){
			mCommentCountTV = (TextView)mView.findViewById(R.id.intro_comment_item_comment_count_tv);
		}
		return mCommentCountTV;
	}

	public LinearLayout getmCommentLL() {
		if(mCommentLL == null){
			mCommentLL = (LinearLayout)mView.findViewById(R.id.intro_comment_item_comment_ll);
		}
		return mCommentLL;
	}

	public TextView getmSubCommentTV() {
		if(mSubCommentTV == null){
			mSubCommentTV = (TextView)mView.findViewById(R.id.intro_comment_item_sub_comment_tv);
		}
		return mSubCommentTV;
	}

	public Button getmPackUpIBtn() {
		if(mPackUpBtn == null){
			mPackUpBtn = (Button)mView.findViewById(R.id.intro_comment_item_pack_up_btn);
		}
		return mPackUpBtn;
	}

//	public ImageButton getmCommentBtn() {
//		if(mCommentBtn == null){
//			mCommentBtn = (ImageButton)mView.findViewById(R.id.intro_comment_item_comment_ibtn);
//		}
//		return mCommentBtn;
//	}

	public ProgressBar getmPB() {
		if(mPB == null){
			mPB = (ProgressBar)mView.findViewById(R.id.intro_comment_item_pb);
		}
		return mPB;
	}

	public TextView getmEmptyTV() {
		if(mEmptyTV == null){
			mEmptyTV = (TextView)mView.findViewById(R.id.intro_comment_item_empty_tv);
		}
		return mEmptyTV;
	}

	public LinearLayout getmSubCommentLL() {
		if(mSubCommentLL == null){
			mSubCommentLL = (LinearLayout)mView.findViewById(R.id.intro_comment_item_ll);
		}
		return mSubCommentLL;
	}
	
}
