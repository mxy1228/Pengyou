package com.cyou.mrd.pengyou.viewcache;

import com.cyou.mrd.pengyou.R;

import android.view.View;
import android.widget.Button;
import android.widget.ImageView;
import android.widget.TextView;

public class GuessGameItemViewCache {

	private View mView;
	private ImageView mIconIV;
	private TextView mNameTV;
	private TextView mPlayNumTV;
	private Button mDownloadBtn;
	
	public GuessGameItemViewCache(View view){
		this.mView = view;
	}

	public ImageView getmIconIV() {
		if(mIconIV == null){
			mIconIV = (ImageView)mView.findViewById(R.id.guess_game_item_icon_iv);
		}
		return mIconIV;
	}

	public TextView getmNameTV() {
		if(mNameTV == null){
			mNameTV = (TextView)mView.findViewById(R.id.guess_game_item_name_tv);
		}
		return mNameTV;
	}

	public TextView getmPlayNumTV() {
		if(mPlayNumTV == null){
			mPlayNumTV = (TextView)mView.findViewById(R.id.guess_game_item_playnum_tv);
		}
		return mPlayNumTV;
	}

	public Button getmDownloadBtn() {
		if(mDownloadBtn == null){
			mDownloadBtn = (Button)mView.findViewById(R.id.guess_game_item_download_btn);
		}
		return mDownloadBtn;
	}
	
	
}
