package com.madhouse.android.trackingcode;

/**
 * @Package: com.madhouse.android.trackingcode
 * @ClassName: SmartmadTrackingCodeListener
 * @Description: Internal interface is described in Server and Native between
 *               the control code transmission interface
 * 
 */
public interface SmartmadTrackingCodeListener {
	/**
	 * Callback tracking status code to listener.
	 * 
	 * @return void
	 * @param statusCode
	 *            Tracking status code.
	 */
	public void onTrackingStatus(int statusCode);
}
